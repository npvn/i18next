import i18next from './i18next.js';

// Forcing the default export to be added as a `default` key, allowing the module
// to be imported using ESM syntax in projects having `esModuleInterop` disabled.
export const forceDefaultExportedAsNamedModule = true;

export default i18next;
